# คัดลอกไฟล์นี้เป็น .env (วางที่ root ของ repo) แล้วใส่ค่า key จริง
# เอา key จาก: Google Cloud Console → เปิดใช้ "Places API (New)" → Credentials → API key
# ⚠️ อย่า commit .env ขึ้น git (ใส่ .env ใน .gitignore)

GOOGLE_PLACES_KEY=ใส่_key_ที่นี่
